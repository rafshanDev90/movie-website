{
  "name": "movie-website",
  "version": "1.0.0",
  "description": "Movie website - Admin uploads, users browse/watch/download",
  "type": "module",
  "main": "backend/server.js",
  "scripts": {
    "dev": "NODE_ENV=development nodemon backend/server.js",
    "start": "NODE_ENV=production node backend/server.js",
    "build:admin": "npm install --prefix admin && npm run build --prefix admin",
    "build:client": "npm install --prefix client && npm run build --prefix client",
    "build": "npm run build:admin && npm run build:client"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.5.1",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
